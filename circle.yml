machine:
  ruby:
    version: 2.3.0

dependencies:
  override:
    - gem install bundler
    - gem install rake
    - bundle install

test:
  override:
    - mkdir Sunomono_test
    - bundle exec rake spec